syntax = "proto3";

message SprintNode {
    string project_name = 1;
    //list of names of task items
    repeated string task_names = 2;
}

message SprintNodeContainer {
    repeated SprintNode entries = 1;
}
